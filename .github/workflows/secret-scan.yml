name: Secret Scan
on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
  # Manuell ausloesbar - siehe Begruendung in typecheck.yml. gitleaks prueft ohnehin
  # den gesamten Baum (--source .), der Lauf haengt an keinem Ereigniskontext.
  workflow_dispatch:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install gitleaks
        run: |
          curl -sSfL https://github.com/gitleaks/gitleaks/releases/download/v8.18.4/gitleaks_8.18.4_linux_x64.tar.gz | tar -xz
          sudo mv gitleaks /usr/local/bin/
      - name: Run gitleaks
        run: gitleaks detect --source . --redact --exit-code 1
